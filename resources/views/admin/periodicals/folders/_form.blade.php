@php
    $folder = $periodicalFolder ?? null;
    $programs = $programs ?? collect();
    $selectedProgramId = old('periodical_program_id', $folder?->periodical_program_id ?? ($selectedProgramId ?? ''));
@endphp
<div class="program-form"><div class="form-section"><div class="section-heading"><span class="section-icon"><i class="bi bi-folder-fill"></i></span><div><h5>Folder Information</h5><p>Enter the folder title, program, and link.</p></div></div><div class="row g-4"><div class="col-lg-6"><label for="periodical_program_id" class="form-label">Program <span>*</span></label><select name="periodical_program_id" id="periodical_program_id" class="form-select @error('periodical_program_id') is-invalid @enderror" required><option value="">Select program</option>@foreach($programs as $program)<option value="{{ $program->id }}" @selected((string)$selectedProgramId === (string)$program->id)>{{ $program->title }}</option>@endforeach</select>@error('periodical_program_id')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="col-lg-6"><label for="title" class="form-label">Folder Title <span>*</span></label><input type="text" name="title" id="title" value="{{ old('title', $folder?->title) }}" class="form-control @error('title') is-invalid @enderror" required>@error('title')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="col-12"><label for="description" class="form-label">Description <small>Optional</small></label><textarea name="description" id="description" rows="4" class="form-control @error('description') is-invalid @enderror">{{ old('description', $folder?->description) }}</textarea>@error('description')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="col-lg-8"><label for="folder_link" class="form-label">Folder Link <span>*</span></label><input type="url" name="folder_link" id="folder_link" value="{{ old('folder_link', $folder?->folder_link) }}" class="form-control @error('folder_link') is-invalid @enderror" required>@error('folder_link')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="col-lg-2"><label for="status" class="form-label">Visibility</label><select name="status" id="status" class="form-select @error('status') is-invalid @enderror"><option value="1" @selected(old('status', $folder?->status ?? 1) == 1)>Active</option><option value="0" @selected(old('status', $folder?->status ?? 1) == 0)>Hidden</option></select>@error('status')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="col-lg-2"><label for="sort_order" class="form-label">Sort</label><input type="number" name="sort_order" id="sort_order" min="0" value="{{ old('sort_order', $folder?->sort_order ?? 0) }}" class="form-control @error('sort_order') is-invalid @enderror">@error('sort_order')<div class="invalid-feedback">{{ $message }}</div>@enderror</div></div></div><div class="form-actions"><a href="{{ route('admin.periodical-folders.index') }}" class="btn-cancel"><i class="bi bi-arrow-left"></i> Cancel</a><button type="submit" class="btn-save"><i class="bi {{ $folder ? 'bi-check-lg' : 'bi-plus-lg' }}"></i> {{ $folder ? 'Update Folder' : 'Create Folder' }}</button></div></div>
